<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <Nullable>disable</Nullable>
    <IsPackable>false</IsPackable>
    <RootNamespace>NFMarketReplayRecorder.Tests</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/NFMarketReplayRecorder.Core/NFMarketReplayRecorder.Core.csproj" />
    <ProjectReference Include="../../src/NFMarketReplayRecorder.Harness/NFMarketReplayRecorder.Harness.csproj" />
    <ProjectReference Include="../../tools/NFMarketReplayRecorder.ApiProbe/NFMarketReplayRecorder.ApiProbe.csproj" />

    <!--
      The ATAS adapter, in STUB mode. Referenced so the stub's fidelity to the
      measured ATAS API is guarded by tests rather than by review: the duplicate
      enum declarations that broke the first real compile are asserted here.
    -->
    <ProjectReference Include="../../src/NFMarketReplayRecorder.ATAS/NFMarketReplayRecorder.ATAS.csproj" />
  </ItemGroup>

</Project>
